apiVersion: v1
kind: Service
metadata:
  name: nginx-proxy-service
  labels:
    app: nginx
    tier: frontend
    lab: lab4
spec:
  type: NodePort
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      nodePort: 30090
  selector:
    app: nginx
    tier: frontend

